# Исправление сохранения ID-based каналов в БД

## Проблема

После добавления поддержки приватных каналов (без username) бот успешно распознавал такие каналы, но при попытке анализа возникала ошибка:

```
⚠️ Не удалось выполнить анализ: 
Не удалось определить username канала — невозможен анализ.
```

### Причина

В функции `save_channel` (db/repo.py) была жёсткая проверка на наличие username:

```python
username = channel_data.get("username")
if not username:
    raise ValueError("Не удалось определить username канала — невозможен анализ.")
```

Для ID-based каналов `username = None`, поэтому возникала ошибка.

### Контекст

В модели БД поле `username` обязательное (`nullable=False`) и уникальное:

```python
class Channel(Base):
    username = Column(Text, unique=True, nullable=False)
```

Это сделано специально, так как username используется как основной идентификатор канала в приложении.

## Решение

### Стратегия: Использование ID как username

Для каналов без username используем их **ID канала** в качестве username в БД с префиксом `id:`.

**Формат:** `id:-1002508742544`

**Преимущества:**
- ✅ Уникальность гарантирована (ID уникален в Telegram)
- ✅ Совместимость с существующей структурой БД
- ✅ Легко различать ID-based от обычных каналов
- ✅ Не требует изменения схемы БД

### Изменённые файлы

#### 1. `app/db/repo.py`

**Функция `save_channel`:**

```python
username = channel_data.get("username")

# Если username нет - используем ID канала
if not username:
    channel_id_from_data = channel_data.get("id")
    if not channel_id_from_data:
        raise ValueError("Не удалось определить username или ID канала")
    # Используем ID как username с префиксом
    username = f"id:{channel_id_from_data}"
    logger.info(f"Channel without username, using ID: {username}")
else:
    username = username.strip().lstrip("@")
```

**Функция `get_channel_id_by_username`:**

```python
identifier = username.strip().lstrip("@")

# Если это ID канала (число), добавляем префикс
if identifier.lstrip('-').isdigit():
    identifier = f"id:{identifier}"

# Ищем в БД
result = await session.execute(
    select(Channel.id).where(Channel.username == identifier)
)
```

#### 2. `app/services/workflow_pipeline.py`

**Формирование имени файла:**

```python
# Определяем имя для отчёта
if channel_data.get("username"):
    username_clean = channel_data["username"].lstrip("@")
else:
    # Для каналов без username используем ID
    username_clean = f"channel_{channel_data.get('id', username)}"

report_path = await generate_similar_channels_xlsx(username_clean)
```

#### 3. `app/services/helpers.py`

**Функция `build_channel_summary`:**

```python
identifier = username.strip().lstrip("@")

# Если это ID канала (число), добавляем префикс "id:"
if identifier.lstrip('-').isdigit():
    identifier = f"id:{identifier}"

# Поиск в БД по правильному идентификатору
result = await session.execute(
    select(Channel, KeywordsCache)
    .where(Channel.username == identifier)
)
```

**Красивое отображение в summary:**

```python
# Для ID-based каналов (username начинается с "id:")
if ch.username.startswith("id:"):
    channel_display = f"<b>{ch.title or 'Приватный канал'}</b>\n🆔 <code>{ch.username}</code>"
else:
    channel_display = f"<b>@{ch.username}</b>"

text = (
    "━━━━━━━━━━━━━━━━━━\n"
    f"📊 {channel_display}\n"
    "━━━━━━━━━━━━━━━━━━\n"
    ...
)
```

#### 4. `app/services/usecases/channel_service.py`

**Поддержка ID в usecase:**

```python
# В analyze_usecase:
identifier = channel.get("username") or str(channel.get("id"))
channel_id = await get_channel_id_by_username(identifier)

# В add_channel_usecase:
identifier = channel_data.get("username") or f"ID:{channel_data.get('id')}"
logger.info("Add channel done identifier=%s", identifier)
```

## Примеры работы

### Пример 1: Обычный канал

```
Telegram API → username = "tech_news"
БД → username = "tech_news"
Отчёт → report_tech_news.xlsx
Summary → 📊 @tech_news
```

### Пример 2: ID-based канал (TERMINAL)

```
Telegram API → username = None, id = -1002508742544
БД → username = "id:-1002508742544"
Отчёт → report_channel_-1002508742544.xlsx
Summary → 📊 TERMINAL
          🆔 id:-1002508742544
```

## Миграция данных

**Не требуется!** Решение полностью обратно совместимо:
- ✅ Старые каналы с username продолжают работать
- ✅ Новые ID-based каналы сохраняются с префиксом `id:`
- ✅ Уникальность username сохраняется

## Проверка работы

### 1. Проверка сохранения в БД:

```sql
-- Обычные каналы
SELECT username, title FROM channels WHERE username NOT LIKE 'id:%';

-- ID-based каналы
SELECT username, title FROM channels WHERE username LIKE 'id:%';
```

### 2. Проверка через бота:

```
1. Переслать пост из приватного канала (например, "TERMINAL")
2. Бот должен показать: "ID: -1002508742544"
3. Выбрать количество каналов (например, 50)
4. Ожидать: "✅ Анализ завершён!"
5. Проверить отчёт: должен прийти файл Excel
```

### 3. Проверка логов:

```bash
grep "Channel without username" logs/bot.log
```

Ожидаемый вывод:
```
Channel without username, using ID: id:-1002508742544
Add channel done identifier=ID:-1002508742544 channel_id=123 posts=50
WF: pipeline finished channel_id=123 report=...
```

## Форматы данных

### В channel_data из Telethon:

**Обычный канал:**
```json
{
  "id": -1001234567890,
  "username": "tech_news",
  "title": "Tech News",
  "about": "...",
  "participants_count": 5000
}
```

**ID-based канал:**
```json
{
  "id": -1002508742544,
  "username": null,
  "title": "TERMINAL",
  "about": "...",
  "participants_count": 1000
}
```

### В БД (таблица channels):

| id | username | title | subscribers |
|----|----------|-------|-------------|
| 1 | tech_news | Tech News | 5000 |
| 2 | id:-1002508742544 | TERMINAL | 1000 |

### В отчётах:

- Обычный: `report_tech_news.xlsx`
- ID-based: `report_channel_-1002508742544.xlsx`

## Ограничения

### 1. Уникальность ID

ID канала должен быть уникальным в БД. Если канал уже существует с таким ID, будет выполнен UPDATE существующей записи.

### 2. Формат ID

ID канала в Telegram всегда число (может быть отрицательным). Префикс `id:` добавляется автоматически.

### 3. Поиск каналов

Поиск ID-based каналов выполняется по формату `id:CHANNEL_ID`. При передаче просто числа оно автоматически преобразуется в этот формат.

## Тестирование

### Тест 1: Сохранение ID-based канала

```python
channel_data = {
    "id": -1002508742544,
    "username": None,
    "title": "TERMINAL",
    "about": "Test channel",
    "participants_count": 100
}

channel_id = await save_channel(channel_data)
# Ожидание: успешное сохранение с username="id:-1002508742544"
```

### Тест 2: Поиск ID-based канала

```python
# По ID
channel_id = await get_channel_id_by_username("-1002508742544")

# По полному формату
channel_id = await get_channel_id_by_username("id:-1002508742544")

# Оба должны найти канал
```

### Тест 3: Полный пайплайн

```python
# Анализ ID-based канала
report = await run_full_pipeline_usecase("-1002508742544", top_n=50)
# Ожидание: файл отчёта создан
```

## Совместимость

- ✅ **Обратная совместимость:** все старые функции работают
- ✅ **API совместимость:** не требуются изменения в API
- ✅ **БД совместимость:** не требуется миграция
- ✅ **UI совместимость:** правильное отображение обоих типов

## Решённые проблемы

1. ✅ Ошибка "Не удалось определить username канала"
2. ✅ Невозможность сохранить ID-based канал в БД
3. ✅ Невозможность выполнить анализ приватного канала
4. ✅ Отсутствие отображения ID-based каналов в summary
5. ✅ Ошибка "Канал @channel_2508742544 не найден в БД" (поиск с неправильным идентификатором)

## Известные проблемы

Нет известных проблем.

## Дальнейшие улучшения

- [ ] Добавить индекс по username для ускорения поиска
- [ ] Кэширование соответствия ID ↔ данные канала
- [ ] Поддержка массового импорта ID-based каналов
- [ ] Визуализация типа канала (ID-based vs username-based) в админке

## Итог

✅ **Проблема полностью решена!**

Теперь бот может:
- Сохранять ID-based каналы в БД
- Выполнять полный анализ приватных каналов
- Корректно отображать ID-based каналы в отчётах
- Работать с обоими типами каналов в едином интерфейсе

**Готово к использованию!** 🎉
