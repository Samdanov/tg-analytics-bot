# 🔧 ИСПРАВЛЕНИЕ SIMILARITY - ФИНАЛЬНЫЙ ПЛАН

## 🐛 Корень проблемы

**Keywords в базе - мусор из Excel импорта:**

```
@Kafka_mems: ['kafka', 'цитадели', 'fps', 'астраномикон', 'https']
@novostyv: ['россия', 'новости', '6456350274', 'novostyv']
@coin_post: ['coin', 'post', 'coinpost', 'деньги']
```

**Причина:** `excel_importer.py` извлекал случайные слова из title/description
вместо использования категории.

**Результат:** Similarity работает по мусорным токенам → нерелевантные совпадения.

---

## ✅ Решение

### Шаг 1: Обновлён excel_importer.py

Новый алгоритм `extract_keywords_v2`:
1. **Приоритет: категория** → тематические ключевые слова
2. Маппинг 48 категорий → ~7 тематических слов каждая
3. Фильтрация мусора (nan, https, telegram, числа)

**Пример (ДО):**
```
@bezposhady: ['bezposhady', 'банкстер', 'беспощадный', '1153820']
```

**Пример (ПОСЛЕ):**
```
@bezposhady [Экономика]: ['Экономика', 'финансы', 'рынок', 'банки', 'инвестиции', 'валюта', 'макроэкономика']
```

---

### Шаг 2: Переимпорт keywords

```bash
cd /home/alex/apps/tg-analytics-bot
source venv/bin/activate

# Переимпорт всех keywords (кроме LLM-анализированных)
python reimport_database.py

# Или с лимитом для теста
python reimport_database.py 10000
```

**Время:** ~5-10 минут для 207К каналов

---

### Шаг 3: Пересчёт similarity

```bash
# Пересчитать similarity для ВСЕХ каналов
python -m app.services.similarity_engine.cli seq 500
```

**Время:** ~30-60 минут для 207К каналов

---

## 📊 Ожидаемый результат

### ДО (мусорные keywords):
```
@bezposhady (экономика):
  18% @a_finance_news
  17% @ouszn (Социальная сфера)
  14% @v_vopros (Важный вопрос)
  14% @posobie_lgota (Пособия и льготы)
```

### ПОСЛЕ (тематические keywords):
```
@bezposhady [Экономика]:
  85% @spydell_finance (финансы, экономика)
  82% @banksta (банки, финансы)
  78% @finanz_ru (экономика, рынок)
  75% @economika_official (экономика, политика)
```

---

## 🚀 ПЛАН ВЫПОЛНЕНИЯ

### Вариант A: Быстрый тест (10 мин)

```bash
# 1. Обновить 10К каналов
python reimport_database.py 10000

# 2. Перезапустить бота
sudo systemctl restart orbita-bot

# 3. Проверить на @bezposhady
# Отправить боту канал, запросить 100 результатов
```

---

### Вариант B: Полный переимпорт (1 час)

```bash
# 1. Остановить бота
sudo systemctl stop orbita-bot

# 2. Обновить ВСЕ keywords
python reimport_database.py

# 3. Пересчитать similarity
python -m app.services.similarity_engine.cli seq 500

# 4. Запустить бота
sudo systemctl start orbita-bot
```

---

## 📋 Категории → Keywords маппинг

| Категория | Keywords |
|-----------|----------|
| Новости и СМИ | новости, СМИ, медиа, журналистика |
| Политика | политика, власть, государство, выборы |
| Экономика | экономика, финансы, рынок, банки |
| Бизнес и стартапы | бизнес, стартап, предпринимательство |
| Криптовалюты | криптовалюта, биткоин, блокчейн |
| Технологии | IT, программирование, разработка |
| ...и ещё 42 категории... | |

---

## ⚠️ Важно

1. **LLM-анализированные каналы НЕ затронуты** 
   - У них audience заполнен
   - Их keywords качественные

2. **Переимпорт безопасен**
   - Обновляются только keywords_json
   - Каналы и посты не удаляются

3. **После переимпорта нужен пересчёт similarity**
   - Иначе старые результаты останутся

---

## 🎯 Рекомендация

**Начни с Варианта A (быстрый тест):**

```bash
cd /home/alex/apps/tg-analytics-bot
source venv/bin/activate

# Обновить 10К каналов
python reimport_database.py 10000

# Перезапустить бота
sudo systemctl restart orbita-bot
```

Проверь результат в Telegram, потом делай полный переимпорт.

---

*Документ создан: 13 декабря 2025*
