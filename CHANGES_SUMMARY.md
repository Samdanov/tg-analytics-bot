# Сводка изменений в тг-боте

## Дата: 21 декабря 2025

### Проблема
Бот всегда выдавал 500 каналов независимо от выбранной кнопки (10, 25, 50, 100, 500).

### Причина
В файле `app/services/xlsx_generator.py` на строке 252 использовалось жестко заданное ограничение `[:500]`, а параметр `top_n` вообще не передавался в функцию `generate_similar_channels_xlsx()`.

## Внесенные изменения

### 1. Исправлена передача параметра `top_n`

#### `app/services/xlsx_generator.py`
- ✅ Добавлен параметр `top_n` в функцию `generate_similar_channels_xlsx()`
- ✅ Заменено `filtered_similar_list[:500]` на `filtered_similar_list[:top_n]`
- ✅ Заменена колонка "Релевантность, %" на "Степень схожести"
- ✅ Проценты заменены на текстовые описания:
  - **Очень высокая** (≥80%) - зеленый цвет
  - **Высокая** (≥60%) - светло-зеленый цвет
  - **Средняя** (≥40%) - оранжевый цвет
  - **Низкая** (<40%) - красный цвет

#### `app/services/use_cases/analyze_channel.py`
- ✅ Добавлена передача `top_n` в `generate_similar_channels_xlsx(filename, top_n=top_n)`

#### `app/services/use_cases/website_service.py`
- ✅ Добавлен параметр `top_n` в `generate_website_similar_channels_xlsx()`
- ✅ Добавлена передача `top_n` в вызов функции
- ✅ Обновлена логика для использования текстовых описаний вместо процентов
- ✅ Добавлено ограничение `[:top_n]` при генерации отчета

### 2. Заменены кнопки на текстовый ввод

#### `app/bot/handlers/workflow_di.py`
- ✅ Добавлен механизм `_pending_requests` для хранения ожидающих анализа запросов
- ✅ Функция `detect_content_handler` теперь запрашивает у пользователя ввод числа от 1 до 500
- ✅ Добавлена функция `process_top_n_input()` для обработки ввода пользователя
- ✅ Добавлены функции `analyze_channel_direct()` и `analyze_website_direct()` для запуска анализа напрямую
- ✅ Добавлена валидация ввода (число от 1 до 500)
- ✅ Добавлена автоматическая очистка устаревших запросов (TTL 5 минут)

#### `app/bot/styles.py`
- ✅ Функция `create_analysis_buttons()` помечена как DEPRECATED (больше не используется)

### 3. Обновлено отображение степени схожести

Вместо процентов релевантности теперь используются:
- Текстовые описания: "Очень высокая", "Высокая", "Средняя", "Низкая"
- Цветовая индикация:
  - 🟢 Очень высокая (зеленый #00B050)
  - 🟢 Высокая (светло-зеленый #92D050)
  - 🟠 Средняя (оранжевый #FFC000)
  - 🔴 Низкая (красный #FF6B6B)
- Белый жирный текст на цветном фоне для лучшей читаемости

## Новый workflow пользователя

1. Пользователь отправляет ссылку на канал или сайт
2. Бот обнаруживает контент и спрашивает: "Введите количество похожих каналов для анализа (от 1 до 500)"
3. Пользователь вводит число, например: `25`
4. Бот запускает анализ и генерирует отчет с указанным количеством каналов
5. В отчете каналы отображаются со степенью схожести (текст + цвет) вместо процентов

## Обратная совместимость

- Callback-обработчики для proxy-каналов (`analyze:`, `force_analyze:`, `analyze_website|`) остались без изменений
- Старые функции помечены как DEPRECATED, но не удалены для обратной совместимости

### 4. Заменен столбец "Дата создания" на нумерацию

#### Проблема
Столбец "Дата создания" показывал дату заполнения БД, а не дату создания канала в Telegram (т.к. эта информация не сохраняется в БД).

#### Решение
- ✅ Заголовок "Дата создания" → "№"
- ✅ Вместо даты теперь порядковый номер (1, 2, 3, ...)
- ✅ Изменено в обоих генераторах:
  - `app/services/xlsx_generator.py`
  - `app/services/use_cases/website_service.py`

**Новая структура таблицы:**
| № | Степень схожести | Ссылка на канал | Подписчики | Название канала | Описание канала |

### 5. Добавлено взвешивание ключевых слов по позиции

#### Проблема
Все ключевые слова имели равный вес при поиске похожих каналов. "Собака" (главная тема) и "корм" (контекст) вносили одинаковый вклад в similarity score.

#### Решение
Реализована система **взвешивания по позиции**:
- **1-е ключевое слово** (главная тема) → вес **1.00** (100%)
- **2-е ключевое слово** → вес **0.83** (83%)
- **5-е ключевое слово** → вес **0.48** (48%)
- **Последнее слово** (контекст) → вес **0.30** (30%)

#### Измененные файлы:
- ✅ `app/services/similarity_engine/shared.py` - функция `calculate_position_weights()`
- ✅ `app/services/similarity_engine/engine.py` - weighted TF в batch расчете
- ✅ `app/services/similarity_engine/engine_single.py` - weighted TF для single channel

#### Преимущества:
- 🎯 **Более точная выдача** - совпадение по главным темам важнее контекста
- 📊 **Учет важности** - первые ключи получают больший вес при сравнении
- 🔄 **Обратная совместимость** - работает с существующими данными
- ⚡ **Автоматически** - не требует изменения LLM промпта

**Пример:**
```
Канал про дрессировку собак:
  ["собака": 1.0, "дрессировка": 0.83, "питомец": 0.65, "команда": 0.48, "воспитание": 0.30]

Совпадение с каналом про собак: "собака" (1.0 × 1.0) = 1.0 → высокий score ✅
Совпадение с вет.каналом: "собака" (1.0 × 0.3) = 0.3 → низкий score ✅
```

**Документация:** См. `KEYWORD_WEIGHTING.md`

### 6. Добавлена система подписок и лимитов

#### Проблема
Нет ограничений на использование бота. Нужна подготовка к монетизации.

#### Решение
Реализована **полная система подписок**:

**Типы подписок:**
- 🆓 **Free** - 10 запросов, до 100 каналов в отчете
- 💎 **Premium** - безлимитные запросы, до 500 каналов
- 👑 **Admin** - безлимит + управление пользователями

**Компоненты:**
- ✅ `app/db/models.py` - модель `User` (user_id, subscription_type, queries_used, etc)
- ✅ `app/services/user_service.py` - сервис управления пользователями
- ✅ `app/bot/middlewares/subscription.py` - автоматическая проверка лимитов
- ✅ `app/bot/handlers/subscription.py` - команды `/stats`, `/admin_*`
- ✅ `scripts/migrate_add_users_table.py` - миграция БД

**Функции:**
- 🔄 Автоматическая регистрация новых пользователей (Free)
- 🚫 Блокировка при исчерпании лимита
- 📊 Учет использованных запросов
- 🎯 Ограничение количества каналов в отчете
- 👑 Admin команды для управления подписками

**Команды:**
```bash
# Для пользователей
/stats               # Статистика и лимиты

# Для админов
/admin_grant <id> premium 30  # Выдать Premium на 30 дней
/admin_reset <id>             # Сбросить счетчик запросов
/admin_info <id>              # Информация о пользователе
/admin_help                   # Справка
```

**Готовность к монетизации:**
- ✅ Инфраструктура готова
- ✅ Легко интегрируется с платежными системами (YooKassa, Stripe, Telegram Stars)
- ✅ Поддержка временных подписок с автоматической проверкой
- ✅ Система банов и управления доступом

**Документация:**
- 📖 `SUBSCRIPTION_SYSTEM.md` - полная документация
- 🚀 `QUICK_START_SUBSCRIPTION.md` - быстрый старт

**Установка:**
```bash
# 1. Запустить миграцию
python3 scripts/migrate_add_users_table.py

# 2. Добавить себя в админы (subscription.py → ADMIN_IDS)

# 3. Перезапустить бота
sudo systemctl restart orbita-bot
```

## Тестирование

✅ Синтаксическая проверка всех файлов пройдена успешно
- `app/services/xlsx_generator.py`
- `app/bot/handlers/workflow_di.py`
- `app/services/use_cases/analyze_channel.py`
- `app/services/use_cases/website_service.py`

## Рекомендации для тестирования

1. Отправить ссылку на канал, ввести число 10, проверить что в отчете 10 каналов
2. Отправить ссылку на сайт, ввести число 50, проверить что в отчете 50 каналов
3. Проверить валидацию: попробовать ввести 0, 501, текст вместо числа
4. Проверить отображение степени схожести в Excel (текст + цвет)
5. Проверить что старые запросы очищаются через 5 минут
