# 🔍 Анализ проблемы с similarity engine

**Дата:** 13 декабря 2025  
**Проблема:** Схожесть показывается как 100%, но реально 60-80%

---

## 🐛 Проблема

### Что происходит:

1. **Алгоритм расчёта** (`engine_single.py`, строки 127-130):
```python
score = sum(idf.get(t, 0.0) for t in common)
denom = sqrt(target_len * len(tokens)) or 1.0
score /= denom
```

Это **упрощенная версия косинусного сходства** с IDF-весами.

2. **Нормализация для отображения** (`xlsx_generator.py`, строки 288-295):
```python
# Нормализуем score относительно МАКСИМАЛЬНОГО score
if max_score > 0:
    normalized_score = min(score / max_score, 1.0)
else:
    normalized_score = min(score, 1.0)
    
relevance_percent = round(normalized_score * 100, 1)
```

### ❌ **В чём проблема:**

**Score нормализуется ОТНОСИТЕЛЬНО МАКСИМАЛЬНОГО score, а не абсолютного значения!**

**Пример:**
- Канал A имеет score = 0.5 (реально 50% схожесть)
- Канал B имеет score = 0.3 (реально 30% схожесть)
- Канал C имеет score = 0.6 (реально 60% схожесть)

**После нормализации:**
- Канал C: 0.6 / 0.6 = **100%** (показывается как идеальное совпадение!)
- Канал A: 0.5 / 0.6 = **83%**
- Канал B: 0.3 / 0.6 = **50%**

**Но реально схожесть всего 60%, 50%, 30%!**

---

## 🎯 Почему так сделано?

Это **относительная схожесть** - "лучший из найденных" всегда = 100%.

**Плюсы:**
- Всегда есть градация (лучший → худший)
- Визуально понятно (зеленый = самый релевантный)

**Минусы:**
- ❌ Не показывает абсолютную схожесть
- ❌ 100% может означать "лучший, но всё равно не очень похож"
- ❌ Вводит в заблуждение рекламодателей

---

## ✅ Решения

### Решение 1: Абсолютная нормализация (рекомендуется)

Изменить нормализацию на абсолютную шкалу 0-1.

**Где изменить:** `app/services/xlsx_generator.py`, строки 288-295

**Было:**
```python
if max_score > 0:
    normalized_score = min(score / max_score, 1.0)
else:
    normalized_score = min(score, 1.0)
```

**Стало:**
```python
# Абсолютная нормализация: score уже в диапазоне [0, ~1]
# Для текущего алгоритма можно использовать напрямую
normalized_score = min(score, 1.0)  # Ограничиваем максимум 1.0
```

**Результат:**
- Score 0.6 → 60% (реальная схожесть)
- Score 0.3 → 30% (реальная схожесть)
- Score 0.9 → 90% (реальная схожесть)

---

### Решение 2: Улучшенный алгоритм TF-IDF + Cosine Similarity

Использовать полноценный TF-IDF с косинусным сходством.

**Где изменить:** `app/services/similarity_engine/engine_single.py`

**Текущий алгоритм (упрощенный):**
```python
score = sum(idf.get(t, 0.0) for t in common)
denom = sqrt(target_len * len(tokens)) or 1.0
score /= denom
```

**Улучшенный алгоритм:**
```python
# TF-IDF vectors
target_vector = {t: tokens.count(t) * idf.get(t, 0.0) for t in target_tokens}
candidate_vector = {t: tokens.count(t) * idf.get(t, 0.0) for t in tokens}

# Cosine similarity
dot_product = sum(target_vector.get(t, 0) * candidate_vector.get(t, 0) for t in common)
target_norm = sqrt(sum(v**2 for v in target_vector.values()))
candidate_norm = sqrt(sum(v**2 for v in candidate_vector.values()))
score = dot_product / (target_norm * candidate_norm) if (target_norm * candidate_norm) > 0 else 0.0
```

**Результат:** Score будет в диапазоне [0, 1], где 1 = идентичные каналы

---

### Решение 3: Минимальный порог схожести

Добавить минимальный порог для фильтрации слабо релевантных каналов.

**Где добавить:** `app/services/similarity_engine/engine_single.py`, после строки 130

```python
# Минимальный порог схожести (например, 0.3 = 30%)
MIN_SIMILARITY_THRESHOLD = 0.3

pairs = []
for cid, tokens in cleaned_by_channel.items():
    if cid == target_channel_id:
        continue
    common = target_set.intersection(tokens)
    if not common:
        continue
    score = sum(idf.get(t, 0.0) for t in common)
    denom = sqrt(target_len * len(tokens)) or 1.0
    score /= denom
    
    # Фильтруем по порогу
    if score < MIN_SIMILARITY_THRESHOLD:
        continue
        
    pairs.append((cid, float(score)))
```

**Результат:** Показываются только каналы с реальной схожестью > 30%

---

### Решение 4: Взвешенное совпадение ключевых слов

Учитывать не только наличие общих слов, но и их важность.

**Идея:**
- Слова из категории "аудитория" весят больше
- Слова из категории "тематика" весят больше
- Общие слова ("новости", "обзор") весят меньше

**Требует:** Расширение структуры данных keywords_cache (добавить категории слов)

---

## 📊 Сравнение решений

| Решение | Сложность | Точность | Скорость | Рекомендация |
|---------|-----------|----------|----------|--------------|
| **Абсолютная нормализация** | ⭐ Низкая | ⭐⭐⭐ Хорошая | ⭐⭐⭐ Быстро | ✅ **Да** |
| **TF-IDF + Cosine** | ⭐⭐ Средняя | ⭐⭐⭐⭐ Отличная | ⭐⭐ Средне | ✅ **Да** |
| **Минимальный порог** | ⭐ Низкая | ⭐⭐⭐ Хорошая | ⭐⭐⭐ Быстро | ✅ **Да** |
| **Взвешенные слова** | ⭐⭐⭐ Высокая | ⭐⭐⭐⭐⭐ Отличная | ⭐⭐ Средне | ⏸️ Потом |

---

## 🚀 Рекомендуемый план

### Этап 1: Быстрое исправление (5 мин)

✅ **Абсолютная нормализация**

Изменить `xlsx_generator.py`:
```python
# Убрать относительную нормализацию
normalized_score = min(score, 1.0)
relevance_percent = round(normalized_score * 100, 1)
```

**Результат:** Показывается реальная схожесть

---

### Этап 2: Улучшение алгоритма (30 мин)

✅ **TF-IDF + Cosine Similarity**

Обновить `engine_single.py`:
- Полноценный TF-IDF
- Косинусное сходство
- Score в диапазоне [0, 1]

**Результат:** Более точный расчёт схожести

---

### Этап 3: Фильтрация (10 мин)

✅ **Минимальный порог = 0.3 (30%)**

Добавить в `engine_single.py`:
```python
MIN_SIMILARITY_THRESHOLD = 0.3
if score < MIN_SIMILARITY_THRESHOLD:
    continue
```

**Результат:** Только действительно релевантные каналы

---

## 🔍 Дополнительные улучшения

### A. Настраиваемые параметры

```python
# В config или через UI
SIMILARITY_THRESHOLD = 0.3  # Минимум 30%
MAX_DF_RATIO = 0.3          # Фильтр частых слов
MIN_KEYWORDS = 4            # Минимум ключевых слов
```

### B. Категории каналов

Добавить поле `category` в `Channel`:
- "Бизнес"
- "Технологии"
- "Lifestyle"
- etc.

Фильтровать по категории ДО расчёта similarity.

### C. Показывать общие ключевые слова

В XLSX добавить колонку "Общие темы":
```python
common_keywords = set(target_keywords).intersection(candidate_keywords)
ws.append([
    ...
    ", ".join(list(common_keywords)[:10])  # Топ-10 общих слов
])
```

**Результат:** Рекламодатель видит ПО КАКИМ словам совпадение

---

## 📝 Выводы

### Текущая проблема:
- ✅ Алгоритм работает, но показывает **относительную** схожесть
- ❌ 100% не означает идеальное совпадение
- ❌ Вводит в заблуждение

### Решение:
1. **Быстро (5 мин):** Абсолютная нормализация
2. **Лучше (30 мин):** TF-IDF + Cosine + порог 30%
3. **Идеально (1 час):** + Категории + Общие слова

---

**Рекомендую начать с Этапа 1 (быстрое исправление), затем Этап 2.**

*Анализ: 13 декабря 2025*

