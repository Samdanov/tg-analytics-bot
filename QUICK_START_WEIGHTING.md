# 🚀 Быстрый старт: Взвешивание ключевых слов

## Что это дает?

Теперь при поиске похожих каналов **первые ключевые слова** (главная тема) важнее **последних** (контекст).

**Пример:**
```
Канал про дрессировку собак:
  🔴 "собака" - главная тема (вес 100%)
  🟠 "дрессировка" - важная (вес 83%)
  🟡 "питомец" - средняя (вес 65%)
  🟢 "команда" - второстепенная (вес 48%)
  🔵 "воспитание" - контекст (вес 30%)
```

Совпадение по "собака" → в **3.3 раза** больше влияет на similarity, чем совпадение по "воспитание".

## Как использовать?

### ✅ Автоматически работает

Система **уже активна** для:
- Новых каналов (анализ через бота)
- Пересчета similarity

**Ничего делать не нужно!**

### 🔄 Пересчитать для существующих каналов

Чтобы улучшить выдачу для уже проанализированных каналов:

```bash
cd /home/alex/apps/tg-analytics-bot
source venv/bin/activate

# Вариант 1: Пересчитать ВСЕ каналы (медленно, 10-60 мин)
python3 -m app.services.similarity_engine.cli batch

# Вариант 2: Пересчитать ОДИН канал (быстро, 10-30 сек)
python3 -m app.services.similarity_engine.cli single <CHANNEL_ID>
```

**Найти CHANNEL_ID:**
```bash
python3 scripts/diagnose_similarity.py <username>
# Покажет ID канала
```

## Ожидаемый эффект

### До взвешивания
```
Канал про Python → похожие:
1. Django канал (совпало: "веб")
2. Кулинарный канал (совпало: "рецепт python") ❌
3. Backend канал (совпало: "api")
```

### После взвешивания
```
Канал про Python → похожие:
1. Django канал (совпало: "python", "веб") ✅
2. Backend канал (совпало: "python", "api") ✅
3. FastAPI канал (совпало: "python") ✅
```

Кулинарный канал ушел вниз, т.к. "python" у него на 10-м месте (контекст).

## Проверка работы

### 1. Диагностика ДО пересчета
```bash
python3 scripts/diagnose_similarity.py <username>
```
Запомните топ-10 похожих каналов.

### 2. Пересчет similarity
```bash
python3 -m app.services.similarity_engine.cli single <CHANNEL_ID>
```

### 3. Диагностика ПОСЛЕ
```bash
python3 scripts/diagnose_similarity.py <username>
```
Сравните топ-10. Каналы с совпадением по **главной теме** должны подняться выше.

## FAQ

**Q: Как понять, что это работает?**
A: Запустите диагностику и посмотрите топ-10. Каналы с похожей главной темой должны быть выше.

**Q: Нужно ли переанализировать каналы?**
A: Нет! Keywords остаются прежними. Нужно только пересчитать similarity.

**Q: Сколько времени займет пересчет?**
A: 
- Один канал: 10-30 секунд
- Вся база (1000+ каналов): 10-60 минут

**Q: Можно ли настроить веса?**
A: Да, но это для разработчиков. См. `KEYWORD_WEIGHTING.md` → "Настройка".

## Рекомендации

1. **Сначала исправьте категории** (если есть проблемы)
   - См. `SIMILARITY_FIX.md`
   
2. **Потом пересчитайте similarity** с новыми весами
   - `python3 -m app.services.similarity_engine.cli batch`

3. **Проверьте результаты** через диагностику
   - `python3 scripts/diagnose_similarity.py <username>`

Это даст максимальное улучшение качества! 🎯

## Документация

- 📖 **Полная документация:** `KEYWORD_WEIGHTING.md`
- 🔧 **Исправление категорий:** `SIMILARITY_FIX.md`
- 📋 **Все изменения:** `CHANGES_SUMMARY.md`
